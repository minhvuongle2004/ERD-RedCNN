# Requirements cho Streamlit App (app.py)
# Cài đặt: pip install -r requirements-app.txt

# Core dependencies từ pyproject.toml
torch>=2.0.0
torchvision>=0.15.0
pydicom==3.0.1
numpy>=1.21.0
PyYAML>=6.0
pandas>=1.3.0

# Streamlit và UI
streamlit>=1.28.0

# Image processing
Pillow>=9.0.0
scikit-image>=0.19.0

# ldctbench package (cài từ source)
# Chạy: pip install -e . (từ thư mục gốc của dự án)

